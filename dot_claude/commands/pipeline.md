---
argument-hint: [feature-description]
description: Run the full pipeline workflow (interview -> plan -> implement -> review -> test) with quality gates
---

Start the pipeline workflow for the following feature:

**Feature**: $1

Load and follow the `pipeline-workflow` skill. Execute all stages sequentially with Inspector quality gates between each stage.

Begin with Step 0 (Setup) and proceed through all steps in order. Do not skip any stage.
